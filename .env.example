OPENAI_API_KEY=your_openai_api_key_here
PORT=3015

# Server Host (IP address or hostname the server listens on)
# Use 0.0.0.0 to listen on all interfaces, or a specific IP like 192.168.1.100
HOST=0.0.0.0

# SSL Certificate paths
SSL_KEY_PATH=/path/to/certs/key.pem
SSL_CERT_PATH=/path/to/certs/cert.pem

# OpenAI Model Configuration
# Options: gpt-4o, gpt-4-turbo, gpt-4o-mini, gpt-3.5-turbo
# Recommended: gpt-4-turbo (best balance of accuracy, speed, and cost)
OPENAI_MODEL=gpt-4-turbo
